import setuptools

setuptools.setup(
    name="recsys-mf",
    version="0.0.1",
    description="A toy project to build a baseline Recommendation System with Matrix Factorization using TensorFlow",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)